// constantes que é uma variavel que não muda

// regras para criar variavel

// nao podemos cria constantes com palavras reservadas ex: let let="jjj"

//  constantes precisam ter nomes significativos 

// nao pode começar o nome de uma  constantes com numero ex: let 1nome; da erro
// ultilizamos camelCase

// nao pode modificar o valor de uma  constantes
// nao utilize var sempre utilize  const
// Case-sensitive 

// na variavel const é necessário declarar e inicializar
// de primeira, ela não premite trocar o valor da const porque ela
// como o proprio nome diz é uma constante

const nome = "Eva"
console.log(nome);

// + - * /

const primeiroNumero = 2;
const segundoNumero = 3;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2
let resultadoTriplicado = resultado * 3
resultadoTriplicado = resultadoTriplicado + 5 

console.log(resultado);
console.log(resultadoDuplicado);
console.log(resultadoTriplicado);

// typrof é para saber qual é o ripo da variavel so para ter a certeza

console.log(typeof resultadoDuplicado);








